import AllTimeLeaderboard from "../components/Leaderboard/AllTimeLeaderboard";

const Leaderboard = () => {
    return (
        <div className="min-h-[85vh] relative overflow-hidden p-3">
            <AllTimeLeaderboard />
        </div>
    );
};

export default Leaderboard;